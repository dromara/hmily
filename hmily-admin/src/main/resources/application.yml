server:
   port: 8888
   servlet:
     context-path: /hmily-admin

spring:
   application:
      name: hmily-admin
   profiles:
     active: db

hmily:
  admin :
    userName : admin
    password : admin

compensation:
   application:
      list : account-service,inventory-service,order-service
   serializer :
      support: kryo
   retry :
      max: 10