hmily:
  server:
    configMode: etcd
    appName: xiaoyu
  #  如果server.configMode eq local 的时候才会读取到这里的配置信息.

remote:
  etcd:
    server: 127.0.0.1:2181
    key: test
    timeoutMs: 6000
    fileExtension: yml
