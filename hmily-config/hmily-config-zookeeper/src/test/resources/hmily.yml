hmily:
  server:
    configMode: zookeeper
    appName: xiaoyu
  #  如果server.configMode eq local 的时候才会读取到这里的配置信息.

remote:
  zookeeper:
    serverList: 127.0.0.1:2181
    fileExtension: yml
    path: /hmily/config
    passive: true

